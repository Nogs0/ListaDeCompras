import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        flex: 1,
        fontSize: 20,
        fontWeight: 'bold',
        fontFamily: 'Nunito',
        padding: 10,
        color: 'yellow'
    }
});

export default styles;