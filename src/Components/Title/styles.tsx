import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        fontSize: 20,
        fontWeight: 'bold',
        fontFamily: 'Nunito',
        padding: 10,
        color: 'yellow'
    }
});

export default styles;